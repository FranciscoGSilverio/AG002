# 🧠 Data Classification with Machine Learning

This project trains a **Machine Learning** model to classify numerical data using **scikit-learn** tools.  
The main script performs data preprocessing, trains the model, evaluates its performance, and saves the result for later use.

---

## 📦 Project Structure

```bash
project/
│
├── src/                 # Utility functions
│   ├── data_utils.py
│   ├── model_utils.py
│   └── __init__.py
│
├── train_model.py       # Main training script
├── config.py            # Basic configurations and constants
├── requirements.txt     # Dependencies
└── README.md
```

## 🚀 How to run

### ✅ 1. Create and activate virtual environment

```bash
python3 -m venv venv
source venv/bin/activate
```
### ✅ 2. Install dependencies

```bash
pip install -r requirements.txt
```
### ✅ 3. Run and script to train the model

```bash
python train_model.py
```
